import express, { Express, Request, Response, NextFunction } from "express";

const AuthHealth = async (req: Request, res: Response, next: NextFunction) => {
  try {
    const da = req.body;
  } catch (error) {}
};

export default AuthHealth;
